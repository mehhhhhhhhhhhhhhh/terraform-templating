# You can still use this feature if you like to -- if you prefer ERB, all the "<%= %>" is a bit of a pain for frequently used values.
# (In native Ruby, obviously these become much less useful.)
# You can see these --var values being passed into `general_options` in the plan script.
#   TODO update reference with final script name

variable "domain" {}
variable "env_tag_name" {}
